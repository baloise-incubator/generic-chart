{{ if .Values.networkClusterIP }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "generic-chart.name" . }}
  labels:
{{ include "generic-chart.labels" . | indent 4 }}
{{- if gt (len .Values.networkClusterIPAnnotations) 0 }}
  annotations:
{{- toYaml .Values.networkClusterIPAnnotations | nindent 4 }}
{{- end }}
spec:
  type: ClusterIP
  ports:
  {{- range $key, $val := .Values.networkClusterIP }}
  {{- if $val.servicePort}}
  - name: {{ $key }}    
    port: {{ $val.servicePort }}
    protocol: {{ default "TCP" $val.protocol }}
    targetPort: {{ default $key $val.targetPort }}
  {{- end }}
  {{- end }}
  selector:
    app: {{ include "generic-chart.name" . }}
    release: {{ .Release.Name }}
{{- end }}
